apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-monitoring-devopstech-net
  namespace: istio-ingress
spec:
  secretName: test-monitoring-devopstech-net-crt
  dnsNames:
  - test.monitoring.devopstech.net
  issuerRef:
    name: staging-cluster-issuer
    kind: ClusterIssuer
    group: cert-manager.io
